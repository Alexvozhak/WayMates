# MCP Tool: update_context

**Priority**: 🟡 P1

Создать Facade MCP tool `update_context` для partial updates. Session validation → normalization updates через Dictionaries → Core.updateContext() → response. Требует FEAT-016 (Core updateContext endpoint).
