# DictionariesManager Moderation (admin endpoints)

**Priority**: 🟡 P1

Добавить admin методы в DictionariesManager: getPendingTerms() (unverified terms для review), approveTerm() (SET verified=true), rejectTerm() (DELETE), replaceTerm() (update canonical + aliases). REST endpoints для Admin CLI.
