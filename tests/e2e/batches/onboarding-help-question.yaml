name: onboarding-help-question
description: User asks "how does this work?" after greeting — should get helpful explanation
locale: ru

steps:
  - message: привет
    expect:
      # greeting response with options

  - message: а как это работает?
    expect:
      # should explain, not fall into unknown
      # intent: help or projectUser

  - message: что такое попутчики?
    expect:
      # should explain waymates concept

  - message: ок, полная история
    expect:
      phase: story_gathering
