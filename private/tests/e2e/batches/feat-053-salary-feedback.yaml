name: feat-053-salary-feedback
description: |
  FEAT-053: salary/feedback extraction in cold-start
  Коммит: 40fe75a
locale: ru

steps:
  # === Test: salary + feedback extraction ===
  - message: |
      хочу рассказать историю карьеры
      начинал как junior backend python/sql в стартапе за 50к в год, было сложно первые полгода.
      потом перешёл в middle python/docker за 80к, работа скучноватая если честно.
      сейчас senior python/kubernetes в финтехе, 120к, интересные задачи наконец-то.
      живу в США, гражданин России.
    expect:
      phase: awaiting_plan_confirmation
      check:
        - queue[0].preview contains "50"
        - queue[1].preview contains "80"
        - queue[2].preview contains "120"

  - message: да, всё верно
    expect:
      phase: awaiting_context_confirmation

  - message: да
    expect:
      phase: awaiting_context_confirmation

  - message: да
    expect:
      phase: awaiting_context_confirmation

  - message: да
    expect:
      phase: awaiting_final_confirmation

  - message: сохранить
    expect:
      phase: saved
