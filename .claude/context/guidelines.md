# Наставления

Документ описывает общий стиль и подход к работе в проекте WayMates. Каждое наставление проработано до первопричины — лечим не симптомы, а ставим диагноз.

**Как использовать**: При `/before-rewind` сверяться с этим документом, добавлять новые инсайты из сессии, не дублировать существующие.

---

## 1. LLM Prompts

### Семантика вместо конкретики в промптах

| | |
|---|---|
| **Паттерн ошибки** | Добавляю в промпты: точные инструкции, дословные примеры, цитаты ожидаемого output, шаблоны фраз, хардкод текста на конкретном языке |
| **Первопричина** | Кажется что конкретика поможет LLM. Но: (1) pattern matching вместо понимания смысла, (2) не генерализирует на новые случаи, (3) хрупкость — любое изменение ломает |
| **Правило** | Только семантика: описывай ЧТО и ЗАЧЕМ, не КАК выглядит конкретная фраза. При code review промптов — предлагать правку если вижу примеры/цитаты/шаблоны. Для NLP: "acknowledge goal was deleted" (не "Цель удалена") — LLM сам переведёт на язык locale |
| **Включает Zod** | `.describe()` попадает в JSON schema → LLM видит примеры. `describe("preview: 'Junior в Яндексе'")` → LLM копирует паттерн. Генерировать describe динамически из типов |

### Словари для extraction

| | |
|---|---|
| **Паттерн ошибки** | Не инжектирую словари в extraction промпты |
| **Первопричина** | Не понимаю что LLM не знает канонические значения проекта и будет выдумывать ("Information Technology" вместо "backend") |
| **Правило** | Словари = source of truth. Инжектировать position, domain, skill, industry в промпт. LLM сопоставляет семантически |

### "map to KNOWN X" без проверки buildHints

| | |
|---|---|
| **Паттерн ошибки** | Добавляю "map to KNOWN EDUCATION LEVELS" в field description, но education_level не в buildHints() вызовах |
| **Первопричина** | Предполагаю что словарь инжектируется, не проверяю. Hint без словаря = LLM угадывает |
| **Правило** | "map to KNOWN X" валиден ТОЛЬКО если X есть в `buildHints([..., "x"])`. Чеклист: (1) grep buildHints по ВСЕМ flows, (2) добавить тип в массив, (3) добавить описание в prompt секцию |

### Multilingual input

| | |
|---|---|
| **Паттерн ошибки** | Забываю указать что input может быть на любом языке |
| **Первопричина** | Промпт на английском → LLM ожидает английский input |
| **Правило** | "Response may be in any language" — обязательно для русского/смешанного input |

### Контекст диалога для classification

| | |
|---|---|
| **Паттерн ошибки** | Парсер интента получает только message, не знает phase |
| **Первопричина** | Думаю что intent универсален. Но "хочу senior" = разный intent в разных фазах |
| **Правило** | Передавать phase context в classifier — что бот спросил определяет смысл ответа |

### Intent descriptions требуют тестирования

| | |
|---|---|
| **Паттерн ошибки** | Добавляю фразу в intent description без проверки classification |
| **Первопричина** | "Звучит логично" ≠ "работает". LLM интерпретирует иначе ("выбираю X" → getStory вместо startStory) |
| **Правило** | Перед изменением intent descriptions — проверить через `classifyIntent()`. Тест: `poc/test-intent.ts` |

### NLP message зависит от structured data

| | |
|---|---|
| **Паттерн ошибки** | NLP prompt говорит "сравни goal с adhocContext", но adhocContext не передаётся в response |
| **Первопричина** | Не проверяю что данные для инструкции доступны в structured data. Инструкция бесполезна без данных |
| **Правило** | Инструкция в prompt → проверить что данные есть в response-builder. Иначе LLM не сможет выполнить |

### Zod error message vs field description

| | |
|---|---|
| **Паттерн ошибки** | Zod safeParse возвращает "Required" вместо human-readable description |
| **Первопричина** | `.describe()` не попадает в error message. Zod использует стандартные сообщения ошибок |
| **Правило** | Для human-readable errors создавать маппинг field → message, использовать вместо err.message |

---

## 2. Tests

### Подгонка assertion под код

| | |
|---|---|
| **Паттерн ошибки** | Тест падает → меняю assertion чтобы прошёл |
| **Первопричина** | Хочу быстрый "зелёный" результат. Но: (1) тест = бизнес-требование, (2) меняя assertion — теряю защиту от регрессии, (3) пропускаю потенциальный баг в бизнес-коде |
| **Правило** | Тест падает → исследуй ПОЧЕМУ → часто это баг в бизнес-коде. Цель тестов — выявить баги, не поднять coverage |

### Театральные проверки

| | |
|---|---|
| **Паттерн ошибки** | Пишу бессмысленные assertions типа `expect(x).toBeGreaterThanOrEqual(0)`. Batch тесты с формулировками типа "неа, передумал, отмена" — так не говорят реальные пользователи |
| **Первопричина** | Coverage theater — хочу показать что "протестировано". Подгоняю тесты под код вместо тестирования реального UX |
| **Правило** | Тесты проверяют бизнес-инварианты. Batch тесты — сначала вручную с естественной речью, потом формализовать. Если формулировка неестественна — тест бесполезен |

### Graceful handling для edge cases

| | |
|---|---|
| **Паттерн ошибки** | Для gibberish/unknown input проверяю конкретный LLM output |
| **Первопричина** | Думаю что LLM предсказуем. Но LLM непредсказуем с edge cases |
| **Правило** | Проверяй инварианты системы (не упала, фаза валидна), не конкретный output |

### Фикстуры с актуальными датами

| | |
|---|---|
| **Паттерн ошибки** | Создаю фикстуры с датами 2022 года |
| **Первопричина** | Не думаю о production thresholds (recency filter = 12 месяцев) |
| **Правило** | Даты в фикстурах должны быть в пределах production thresholds |

### Логика тестовых данных

| | |
|---|---|
| **Паттерн ошибки** | User достигает цели раньше Pathfinder в тестовых данных |
| **Первопричина** | Генерирую данные без проверки бизнес-логики |
| **Правило** | Всегда проверять что тестовые данные имеют смысл в бизнес-контексте |

### Negative assertions

| | |
|---|---|
| **Паттерн ошибки** | Тест проверяет только "U5 найден" без проверки "U8 НЕ найден" |
| **Первопричина** | Фокус на happy path |
| **Правило** | Проверять что правильное найдено + что неправильное исключено |

---

## 3. Types/Zod

### Смешивание null/undefined

| | |
|---|---|
| **Паттерн ошибки** | Использую то null, то undefined для "отсутствия значения" |
| **Первопричина** | В JS оба значат "отсутствие". Но: (1) OpenAI structured output требует nullable, не optional, (2) Neo4j возвращает null для отсутствующих полей, (3) JSON не имеет undefined |
| **Правило** | `null` единообразно. Один способ = один источник истины |

### Касты `as Type`

| | |
|---|---|
| **Паттерн ошибки** | Использую `as Type` чтобы "заткнуть" TypeScript |
| **Первопричина** | Быстрее чем исправить типы |
| **Правило** | Каст = сигнал что типы неправильные. Использовать: (1) Zod `.parse()` — предпочтительно, (2) самописный typeguard — на крайняк |

### Weak typing для LLM schemas

| | |
|---|---|
| **Паттерн ошибки** | `z.array(z.string())` для полей с ограниченным набором значений |
| **Первопричина** | Быстрее написать string чем enum |
| **Правило** | `z.array(z.enum([...]))` — LLM видит допустимые значения в JSON schema |

---

## 4. Код и паттерны

### Фаллбэки и врапперы "на всякий случай"

| | |
|---|---|
| **Паттерн ошибки** | Добавляю factory helpers, fallback values, wrapper objects без явной необходимости |
| **Первопричина** | Хочу "безопасность" и "расширяемость". Но: (1) засоряет бизнес-код, (2) глотает ошибки вместо всплытия, (3) прячет реальное поведение глубоко в абстракции |
| **Правило** | YAGNI. Добавляй сложность только когда есть доказанная необходимость |

### Хардкод значений

| | |
|---|---|
| **Паттерн ошибки** | Хардкожу levels, options, значения вместо получения из источника |
| **Первопричина** | Быстрее написать константу |
| **Правило** | Данные из источника: словари из БД, levels из trajectories, config из env |

### Дублирование vs DRY

| | |
|---|---|
| **Паттерн ошибки** | Вижу дублирование → откладываю "на потом" |
| **Первопричина** | Хочу быстрее закончить текущую задачу |
| **Правило** | Видишь дублирование → HOF/abstraction сразу. 1 час на HOF = экономия на поддержке |

### Inline "грязь"

| | |
|---|---|
| **Паттерн ошибки** | `...(condition && { field })` inline в return |
| **Первопричина** | Компактнее |
| **Правило** | Явные переменные или if/else для читаемости. Код читают чаще чем пишут |

### Сложные условия

| | |
|---|---|
| **Паттерн ошибки** | `if (a && b.length > 0 && c !== "x")` без именования |
| **Первопричина** | Кажется очевидным |
| **Правило** | Complex conditions → named variables: `const hasData = ...; const shouldRun = ...;` |

### Имя не соответствует семантике

| | |
|---|---|
| **Паттерн ошибки** | Функция меняет поведение, имя остаётся старым (`createSystemMessage` когда уже не system message) |
| **Первопричина** | Имя — "просто строка", не часть контракта. Но: (1) имя = документация для следующего читателя, (2) неверное имя = ложные ожидания = баги |
| **Правило** | Семантика меняется → имя меняется. Имя — часть контракта |

### Технические названия вместо бизнес-семантики

| | |
|---|---|
| **Паттерн ошибки** | Технические названия для бизнес-концепций: `isArray`, `contextMultiValue`, `useAnyMatch` |
| **Первопричина** | Мыслю реализацией (КАК код работает), не бизнес-логикой (ЧТО это значит) |
| **Правило** | Названия должны отражать бизнес-семантику. `type: "single" | "multi"` (у кандидата одно или много значений) понятнее чем `isArray` (техническая деталь). Спрашивать себя: "Как бы это объяснил продакт-менеджер?" |

### Ручная работа вместо инструментов

| | |
|---|---|
| **Паттерн ошибки** | Ручной cleanup импортов, форматирование, сортировка |
| **Первопричина** | Не знаю/забываю о возможностях инструментов |
| **Правило** | `lint:fix` удаляет unused imports, форматирует код. Инструменты автоматизируют рутину — использовать их |

### Фиксить в правильном слое

| | |
|---|---|
| **Паттерн ошибки** | Workaround в Facade для проблемы в Core (напр. API возвращает неполные данные) |
| **Первопричина** | Быстрее "залатать" на месте чем разобраться где root cause |
| **Правило** | Баг в Core → фикс в Core. Баг в Facade → фикс в Facade. Workarounds накапливают tech debt |

---

## 5. LangGraph

### userResponse не очищается

| | |
|---|---|
| **Паттерн ошибки** | Использую userResponse но не очищаю для следующего node |
| **Первопричина** | Не помню что state reducer `lastValue` сохраняет значения между вызовами. `"" || interrupt()` всегда вызовет interrupt |
| **Правило** | После использования userResponse → `userResponse: ""` в return (кроме случаев когда следующий node должен его получить) |

### Чеклист при добавлении intent

| | |
|---|---|
| **Паттерн ошибки** | Добавляю intent в одно место, забываю другие. Routing function возвращает node которого нет в buildRouteMap |
| **Первопричина** | Не понимаю что intent проходит через несколько слоёв регистрации |
| **Правило** | При добавлении intent проверить ВСЕ 4 места: (1) `state.ts` — SIMPLE_INTENTS/COMPLEX_INTENTS, (2) `parse-intent.ts` — Zod schema, (3) `prompts.ts` — INTENT_DESCRIPTIONS, (4) `search-router.ts` — route map + PARSE_INTENT_ROUTE_MAPS. `buildRouteMap destinations === ВСЕ return values роутера` |

### unknown intent без явного handling

| | |
|---|---|
| **Паттерн ошибки** | Полагаюсь на fallback `??` для unknown intent |
| **Первопричина** | Думаю что LLM всегда вернёт валидный intent |
| **Правило** | Explicit route для `unknown` → `clarify_intent` node. LLM должна иметь путь отступления |

### Debug логи в production коде

| | |
|---|---|
| **Паттерн ошибки** | Добавляю console.log для отладки и забываю удалить |
| **Первопричина** | Тороплюсь исправить баг |
| **Правило** | Debug логи — временные. Добавил → понял → удалил. Для production есть Pino + LangSmith |

### Изменил код — не применил изменения

| | |
|---|---|
| **Паттерн ошибки** | Добавил debug/fix, запустил проверку, не вижу эффекта — забыл rebuild |
| **Первопричина** | Не различаю какой код где выполняется. Тесты могут запускать код in-process или через Docker — разные точки входа |
| **Правило** | После изменения: (1) определить ГДЕ код исполняется (in-process vs container), (2) выполнить соответствующий rebuild. Чеклист: edit → build → deploy → verify |

### Hardcoded строки в промптах

| | |
|---|---|
| **Паттерн ошибки** | Intent names в промпте как строки ("PROCEED", "CLARIFY"), не связаны с enum |
| **Первопричина** | Быстрее написать строку чем сделать type-safe |
| **Правило** | Single source of truth: enum в `state.ts`, промпт интерполирует через `Record<EnumType, string>`. TypeScript ловит рассинхрон на compile time. Для фаз/состояний: `${SEARCH_PHASE.deleting_goal}` вместо строки "deleting_goal" — type-safe + автокомплит |

### Overwrite вместо Merge для incremental input

| | |
|---|---|
| **Паттерн ошибки** | При дополнении данных (adhoc context, goal) извлекаю заново с нуля, теряя уже собранное |
| **Первопричина** | Не думаю о multi-turn flow. Каждое сообщение обрабатываю изолированно |
| **Правило** | LLM merge pattern: передать текущее состояние в промпт (`Current: {json}`), LLM сама решает что обновить/сохранить. Пример: `buildAdhocClarificationPrompt`, `buildGoalClarificationPrompt` |

### Placeholder strings вместо explicit arguments

| | |
|---|---|
| **Паттерн ошибки** | `PROMPT.replace("{field}", value)` — неявная подстановка |
| **Первопричина** | Быстрее написать placeholder чем продумать API функции |
| **Правило** | Builder function с явными аргументами: `buildPrompt(arg1, arg2)`. IDE подсказывает, TypeScript проверяет |

### ask intent не добавлен во все фазы

| | |
|---|---|
| **Паттерн ошибки** | "что ты умеешь?" классифицируется как cancel |
| **Первопричина** | `ask` intent добавлен только в некоторые фазы PHASE_CONTEXT. В остальных LLM не имеет валидного варианта для мета-вопросов |
| **Правило** | `ask` intent должен быть в КАЖДОЙ фазе. Пользователь может задать мета-вопрос в любой момент |

### Prompt показывает все intents, LLM игнорирует Valid constraint

| | |
|---|---|
| **Паттерн ошибки** | LLM выбирает semantically matching intent вместо Valid intent |
| **Первопричина** | Prompt содержит ALL intent descriptions + "Valid: X, Y". LLM делает semantic match, игнорирует constraint |
| **Правило** | Prompt должен показывать ТОЛЬКО valid intents. Router = source of truth. `getValidIntentsForPhase(phase, flags)` |

### proceed ≠ explore (семантика)

| | |
|---|---|
| **Паттерн ошибки** | "глянь похожих" → proceed → wrong routing |
| **Первопричина** | `proceed` = "agrees WITHOUT new info". "глянь похожих" = новая информация (что хочет пользователь) |
| **Правило** | `proceed` = pure confirmation. `explore` = request to see similar people. Разные семантики |

### Advisor как тупик без выхода

| | |
|---|---|
| **Паттерн ошибки** | Advisor имеет только `ask` и `done` интенты, нет выхода в main flow |
| **Первопричина** | Думаю что advisor — финальная точка. Но пользователь может захотеть действие (set goal, filter) из advisor |
| **Правило** | Generic `action` intent в advisor → роутит в parse_search_intent с сохранённым userResponse. Не дублировать интенты — main flow разберётся |

### INTERRUPT ноды нельзя использовать как промежуточные

| | |
|---|---|
| **Паттерн ошибки** | Роучу из advisor в confirm_adhoc_context чтобы "вернуться в flow" |
| **Первопричина** | Не понимаю что interrupt() ВСЕГДА останавливает выполнение и ждёт ввода |
| **Правило** | Для возврата в flow без interrupt — роутить напрямую в PARSE ноду (parse_search_intent). Нужен previousPhase чтобы знать route map |

### Инструкции одной фазы влияют на другие

| | |
|---|---|
| **Паттерн ошибки** | NLP для `confirming_adhoc_context` спрашивает seniority, хотя это инструкция для `asking_adhoc_context` |
| **Первопричина** | Все описания фаз в одном SEARCH_PROMPT. LLM видит все инструкции и может применить не ту |
| **Правило** | Инструкции в PHASE_DESCRIPTIONS должны быть взаимоисключающими. Не "Ask for X", а "DO NOT ask for anything from FILLED section" |

### Manual type alias вместо keyof

| | |
|---|---|
| **Паттерн ошибки** | `type Field = "a" \| "b" \| "c"` дублирует ключи бизнес-типа |
| **Первопричина** | Копировал поля вручную, не думал о связи с бизнес-типом |
| **Правило** | `Record<keyof BusinessType, string>` — single source of truth. При добавлении поля в schema — TypeScript потребует описание |

---

## 6. Cypher/Neo4j

### WITH clause scope

| | |
|---|---|
| **Паттерн ошибки** | Забываю что WITH сбрасывает переменные не в списке |
| **Первопричина** | Не помню семантику Cypher WITH |
| **Правило** | `WITH *` сохраняет все, `WITH x, y` — только x и y. Всегда проверять что нужные переменные пробрасываются |

### Null safety для optional properties

| | |
|---|---|
| **Паттерн ошибки** | `WHERE all(d IN c.domains WHERE ...)` без проверки на null |
| **Первопричина** | Предполагаю что поля заполнены |
| **Правило** | `CASE WHEN x IS NULL THEN true ELSE ... END` для всех optional properties в WHERE |

### Map projection вместо field mapping

| | |
|---|---|
| **Паттерн ошибки** | Маплю поля в TypeScript вместо Cypher |
| **Первопричина** | Привычка |
| **Правило** | `RETURN { field1, field2, computed: expr } AS result` — type conversions в Cypher, один cast в TypeScript |

### LIMIT без GROUP BY

| | |
|---|---|
| **Паттерн ошибки** | `ORDER BY x LIMIT 1` после aggregation — возвращает 1 строку globally |
| **Первопричина** | Ожидаю per-group limit как в SQL |
| **Правило** | Для per-group limit: `collect(...)[0]` внутри aggregation, не LIMIT |

### Debug сложных queries

| | |
|---|---|
| **Паттерн ошибки** | Debug всего query сразу, не понимаю где проблема |
| **Первопричина** | Не хочу тратить время на изоляцию |
| **Правило** | Убирай части query по одной, пока результат не изменится — найдёшь проблемную часть |

---

## 7. Процесс работы

### Pre-Action Declaration

| | |
|---|---|
| **Паттерн ошибки** | Начинаю писать код без полного понимания контекста. Понял проблему через thinking → сразу Edit, без объяснения пользователю |
| **Первопричина** | Хочу показать прогресс. Thinking создаёт иллюзию "я понял = можно делать". Но: (1) переделка дороже чем понимание, (2) пользователь не видит моё reasoning |
| **Правило** | 90%+ уверенность перед действием. Перед изменением объявить: **Проблема** → **Как сейчас** → **Как будет** → **Уверенность** (%). СНАЧАЛА объяснить → ПОТОМ ждать "ок"/"делай" → ПОТОМ Edit. Thinking ≠ объяснение пользователю |

### Делегирование без согласия

| | |
|---|---|
| **Паттерн ошибки** | Использую Task agent без явного разрешения |
| **Первопричина** | Считаю что агент справится лучше |
| **Правило** | Не делегировать без явного согласия. "Давай дальше сам" = работаю сам |

### Файл не найден — не искал

| | |
|---|---|
| **Паттерн ошибки** | Файл не найден по указанному пути → говорю "не найден" без поиска |
| **Первопричина** | Доверяю переданному пути буквально, не проверяю альтернативы |
| **Правило** | Файл не найден → искать по имени через `Glob` (`**/*имя*.md`). Не говорить "не найден" без поиска |

### Не думаю о workflow использования

| | |
|---|---|
| **Паттерн ошибки** | Реализую фичу без понимания как её будут использовать |
| **Первопричина** | Фокус на реализации, не на использовании |
| **Правило** | "А workflow какой будет?" — думать об использовании ДО реализации |

### Закрываю баги самостоятельно

| | |
|---|---|
| **Паттерн ошибки** | Считаю баг исправленным без визуальной верификации пользователем |
| **Первопричина** | Уверен что исправил |
| **Правило** | Визуальная верификация пользователем обязательна. "Баги не закрывай пока я не проверил" |

### Не добавляю в матрицу тестирования

| | |
|---|---|
| **Паттерн ошибки** | Верифицировал баг/фичу через mcp-chat.ts, но не добавил запись в tests_report.md |
| **Первопричина** | Забываю что матрица — trace для регрессии и документация проверенных сценариев |
| **Правило** | После верификации через mcp-chat.ts → добавить строку в "Матрица тестирования" (tests_report.md). Формат: коммит, from phase, input, intent, to phase, result |

### Перехожу к задаче без апрува

| | |
|---|---|
| **Паттерн ошибки** | Завершил одну задачу и сразу начинаю следующую без подтверждения пользователя |
| **Первопричина** | Хочу показать прогресс, кажется что список задач = разрешение делать все |
| **Правило** | Завершил задачу → Pre-Action Declaration для следующей → ждать явный апрув ("да", "ок", "делай"). Не приступать без подтверждения |

### Проверка плана перед реализацией

| | |
|---|---|
| **Паттерн ошибки** | План описывает технический рефакторинг, но бизнес-семантика требует другого. Делаю поля optional потому что "сейчас Cypher не возвращает" |
| **Первопричина** | План фиксирует КАК, не проверяя ЧТО это значит для бизнеса. Мыслю текущим кодом, не целевым состоянием |
| **Правило** | Перед реализацией: (1) читать план ЦЕЛИКОМ, (2) уточнить бизнес-семантику ("matchedContext = куда пришли или где были?"), (3) думать о ЦЕЛЕВОМ состоянии ("после унификации path required" → сразу required) |

---

## 8. Коммуникация

### Угодничество вместо анализа

| | |
|---|---|
| **Паттерн ошибки** | Подтверждаю ожидания пользователя вместо объективного анализа |
| **Первопричина** | Хочу угодить, избегаю конфликта. Но: (1) пользователь теряет возможность пересмотреть решение, (2) технический долг накапливается, (3) моя роль — экспертиза, не поддакивание |
| **Правило** | Честный анализ > подтверждение ожиданий. "Нужен ли MCP?" → анализ плюсов/минусов, не "да, конечно нужен!" |

### Объясняю сложно

| | |
|---|---|
| **Паттерн ошибки** | Использую технические термины без объяснения |
| **Первопричина** | Предполагаю знание веб-архитектуры, TypeScript, LangGraph |
| **Правило** | Объясняй как будто собеседник не знает контекст. "Печатаем JS в текстовый файл" понятнее чем "esbuild bundle injection" |

### Не уточняю неоднозначности

| | |
|---|---|
| **Паттерн ошибки** | Делаю предположения вместо вопросов |
| **Первопричина** | Не хочу "надоедать" вопросами |
| **Правило** | Лучше спросить через AskUserQuestion чем сделать неправильно и переделывать |

---

### Добавление mode в discriminated union

| | |
|---|---|
| **Паттерн ошибки** | Добавил новый mode, но методы делают assumptions о структуре данных старых modes |
| **Первопричина** | Не проследил data flow — какие методы зависят от структуры `trajectories[0]` и т.п. |
| **Правило** | При добавлении mode — grep по всем методам класса/модуля, проверить каждый на assumptions о данных |

### discriminatedUnion: уникальные discriminator values

| | |
|---|---|
| **Паттерн ошибки** | Zod `discriminatedUnion` с двумя вариантами на одном discriminator value |
| **Первопричина** | Хочу разные структуры для одной "фазы" (напр. `showing_exploration` с `needsFiltering: true/false`) |
| **Правило** | Разные discriminator values для разных структур: `showing_exploration_candidates` + `showing_exploration_facets`, не один `showing_exploration` с флагом |

### Schema inconsistency между связанными типами

| | |
|---|---|
| **Паттерн ошибки** | AdhocContext имеет поля (industry, cityName, citizenships) которых нет в TargetContext. Naming: countryCode vs countries |
| **Первопричина** | Типы создавались в разное время, не сверялись друг с другом |
| **Правило** | При создании связанных типов — проверять пересечение полей. Заводить FEAT на несоответствия. Унифицировать naming |

### Два источника message (interrupt vs response-builder)

| | |
|---|---|
| **Паттерн ошибки** | Нода формирует message в interrupt, response-builder тоже формирует message — конфликт |
| **Первопричина** | Не проследил data flow. Предположил что interrupt.message используется, но он игнорируется — используется response-builder |
| **Правило** | Перед изменением проследить ВЕСЬ data flow до UI. Один источник истины для каждого поля |

### Детектирование состояния по косвенным признакам

| | |
|---|---|
| **Паттерн ошибки** | `messages.length > 0` для определения "есть контент", но первое сообщение — intent, не контент |
| **Первопричина** | Не понял СЕМАНТИКУ данных, только структуру. `messages` = всё включая intent |
| **Правило** | При использовании данных для логики — понимать их семантику. Если нужен "карьерный контент" — это не то же что "любые messages" |

### Бизнес-решения без спроса

| | |
|---|---|
| **Паттерн ошибки** | Предлагаю порядок вопросов "companies → dates → transitions" без обоснования |
| **Первопричина** | Додумываю бизнес-логику вместо того чтобы спросить |
| **Правило** | Бизнес-решения (порядки, приоритеты, обязательность полей) — спрашивать явно. Не "мне кажется так лучше", а "какой приоритет?" |

---

### Архитектурные утверждения без верификации

| | |
|---|---|
| **Паттерн ошибки** | "Path обязателен в Cypher для dual matching" — утверждаю с высокой уверенностью, не проверив |
| **Первопричина** | Делаю логический вывод, но не верифицирую по коду. "Звучит правильно" ≠ "проверил и подтвердил" |
| **Правило** | Архитектурные утверждения → читать код → давать оценку уверенности в %. Когда пользователь сомневается — это сигнал перепроверить |

### Не замечаю баги в data flow

| | |
|---|---|
| **Паттерн ошибки** | Не заметил что trails НЕ загружаются для waymates (LLM advisor их не видит) |
| **Первопричина** | Фокус на текущей задаче (DTW), не проверяю смежные data flows |
| **Правило** | При изучении архитектуры — проверять ВСЕ данные которые должны попадать в конечную точку (UI, LLM). "А что ещё должно быть здесь?" |

---

### Breaking changes без deprecated

| | |
|---|---|
| **Паттерн ошибки** | Создаю новую функцию рядом со старой (`buildTrajectoryQuery` + `buildPathQuery`) вместо замены |
| **Первопричина** | Боюсь сломать существующий код. Но: (1) deprecated код накапливается, (2) путаница какую функцию использовать |
| **Правило** | Breaking changes сразу. Новое заменяет старое, не добавляется рядом. Использовать filesystem MCP для batch изменений |

### Магические числа в коде

| | |
|---|---|
| **Паттерн ошибки** | `if (trajectory.length >= 3)` без объяснения почему 3 |
| **Первопричина** | "Это очевидно" — но через месяц никто не помнит почему |
| **Правило** | Бизнес-правила в константы: `DTW_MIN_TRAJECTORY_LENGTH = 3` с комментарием объясняющим ПОЧЕМУ |

### Точечные фиксы вместо full scope

| | |
|---|---|
| **Паттерн ошибки** | Добавил DTW к pathfinders, но не унифицировал архитектуру с waymates |
| **Первопричина** | Фокус на immediate task, не на системном решении |
| **Правило** | Перед фиксом — оценить full scope. "А как это связано с похожими местами?" Лучше один большой рефакторинг чем много точечных |

### Не сравниваю типы перед рефакторингом

| | |
|---|---|
| **Паттерн ошибки** | Предложил "generic метод" не сравнив схемы `ScoredMatchedCandidate` vs `PathfinderCandidate` |
| **Первопричина** | Предполагаю что типы похожи, не проверяю |
| **Правило** | Перед унификацией типов — grep схемы, сравнить поле за полем. Часто можно унифицировать проще чем кажется |

### Grep перед изменением

| | |
|---|---|
| **Паттерн ошибки** | Делаю по-своему вместо консистентности с существующим кодом. Оцениваю scope "50 LOC" когда паттерн уже реализован рядом. Удаляю/добавляю не проверив аналоги |
| **Первопричина** | "Я знаю как надо" вместо "как уже сделано". Планирую с нуля, не смотрю решённые задачи |
| **Правило** | Перед изменением/удалением/добавлением → grep аналогичные места (search-graph = эталон для cold-start, explore.ts для validate-goal.ts). Если паттерн проекта — следовать ему. Часто нужно скопировать 3-5 строк |

### Принимаю "баг" без проверки бизнес-требований

| | |
|---|---|
| **Паттерн ошибки** | Баг #12 "нельзя пропустить citizenships" → сразу предлагаю fix, не спросив "а это баг?" |
| **Первопричина** | "Пользователь сказал баг = баг" |
| **Правило** | Перед фиксом спросить: "Это баг или бизнес-требование?" Обязательные поля могут быть by design |

### UX без продумывания дублирования

| | |
|---|---|
| **Паттерн ошибки** | Показал FILLED в clarification, потом те же данные в confirmation |
| **Первопричина** | Добавляю информацию не думая что пользователь уже видел |
| **Правило** | Перед добавлением UX элемента: "Что пользователь уже видел на предыдущих шагах?" |

### Хардкод строк вместо type-safe

| | |
|---|---|
| **Паттерн ошибки** | `const FIELDS = ["companySize", "birthYear"]` без связи с типом |
| **Первопричина** | Быстрее написать строку чем `keyof Pick<Type, ...>` |
| **Правило** | Всегда связывать с бизнес-типом через `keyof Pick<>`, `Exclude<>`, `satisfies` |

---

### Костыли вместо понимания первопричины

| | |
|---|---|
| **Паттерн ошибки** | Предлагаю `userResponse \|\| clarificationText` вместо понимания почему два поля существуют |
| **Первопричина** | Хочу быстро решить проблему, не копаю в историю. Но костыль накапливает complexity |
| **Правило** | При обнаружении "странного" поля/паттерна — сначала Explore agent для археологии: git history, все места использования, первопричина появления. Потом решать |

### Рефакторинг routing без проверки всех intents

| | |
|---|---|
| **Паттерн ошибки** | Изменил `clarify → load_context`, но не проверил что `proceed → extract_goal` покрывает goal intent |
| **Первопричина** | Фокус на исправляемом пути, не на всей фазе |
| **Правило** | При изменении routing в фазе — проверить ВСЕ intents этой фазы. Составить таблицу intent → node → что делает |

### UX > техническая корректность

| | |
|---|---|
| **Паттерн ошибки** | Предложил "acknowledge briefly" — технически правильно, но UX как "рация" |
| **Первопричина** | Формальное выполнение инструкции без продумывания UX |
| **Правило** | Перед реализацией спросить: "Как это будет ощущаться пользователем?" |

### eslint.config.mjs = source of truth

| | |
|---|---|
| **Паттерн ошибки** | Использовал `as` cast, хотя eslint запрещает (`assertionStyle: 'never'`) |
| **Первопричина** | Не читаю конфиг линтера, пишу как привык |
| **Правило** | Перед `as`, `any`, `!` — проверить eslint.config.mjs. Если запрещено — найти type-safe альтернативу |

### Рефакторинг без оценки бизнес-ценности

| | |
|---|---|
| **Паттерн ошибки** | Предлагаю рефакторинг "для консистентности" без оценки нужен ли он |
| **Первопричина** | "Сделать как там" = автопилот. Консистентность кажется самоценной |
| **Правило** | Перед рефакторингом: (1) Какая бизнес-ценность? (2) Что сломается если НЕ делать? (3) Код работает? → возможно менять не нужно |

### Спрашиваю вместо исследования документации

| | |
|---|---|
| **Паттерн ошибки** | Спрашиваю пользователя про trails вместо чтения GLOSSARY.md, schemas.ts |
| **Первопричина** | Быстрее спросить чем искать. Но: (1) пользователь тратит время, (2) документация точнее устного ответа |
| **Правило** | Сначала искать в документации (GLOSSARY, BUSINESS-LOGIC, schemas). Спрашивать только если не нашёл |

### Решение без альтернатив и обоснования

| | |
|---|---|
| **Паттерн ошибки** | Предлагаю одно решение без объяснения почему именно оно, без альтернатив |
| **Первопричина** | Тороплюсь, кажется очевидным, не хочу перегружать |
| **Правило** | При решении с выбором места правки: (1) контекст проблемы, (2) варианты с плюсами/минусами, (3) рекомендация с обоснованием, (4) степень уверенности |

### Правка без проверки регресса

| | |
|---|---|
| **Паттерн ошибки** | Исправил classifier, не проверил что существующие тесты не сломались |
| **Первопричина** | Фокус на новой функциональности, забываю про существующую |
| **Правило** | После правки в shared коде (classifier, prompts, router) — запустить ВСЕ связанные тесты до и после |

### Правка промпта без проверки схемы

| | |
|---|---|
| **Паттерн ошибки** | Добавил "Unknown Company" в промпт, хотя в UserContext нет companyName |
| **Первопричина** | Правлю промпт не проверив какие поля реально собираются |
| **Правило** | Перед правкой extraction/planning промпта — прочитать целевую Zod schema (UserContext, Trail). Промпт должен соответствовать схеме |

---

### Терминология проекта

| | |
|---|---|
| **Паттерн ошибки** | Использую "profile", "context" когда в проекте "adhocContext", "userContext" |
| **Первопричина** | Использую привычные термины вместо терминов проекта |
| **Правило** | Изучить терминологию проекта (schemas, GLOSSARY). adhocContext ≠ profile ≠ userContext — разные сущности |

### Избыточность в документации

| | |
|---|---|
| **Паттерн ошибки** | Добавляю примеры, когда формат уже определён в существующем файле |
| **Первопричина** | Хочу быть полным, но: (1) дублирование, (2) рассинхрон при изменениях |
| **Правило** | "Примеры уже есть" — ссылаться на существующее, не дублировать |

### Примеры в UX текстах должны быть валидными

| | |
|---|---|
| **Паттерн ошибки** | Предлагаю примеры для пользователя ("senior backend в финтехе") без проверки валидности |
| **Первопричина** | Фокус на читаемости, не на корректности. Не сверяюсь с validation schema |
| **Правило** | Примеры в UX текстах ДОЛЖНЫ быть валидными для extraction. Проверить required fields (position, role, countryCode, domains) перед предложением |

---

### Кросс-ссылки между интентами в промпте

| | |
|---|---|
| **Паттерн ошибки** | "NOT for X — that's Y" в описании intent |
| **Первопричина** | Хочу помочь LLM различить похожие intents |
| **Правило** | Описания интентов должны быть self-contained. Семантика, не отсылки на другие intents |

### Код после interrupt() не выполняется

| | |
|---|---|
| **Паттерн ошибки** | `const result = interrupt({...}); return { chartUrl }` — chartUrl никогда не попадает в state |
| **Первопричина** | Не понимаю что `interrupt()` выбрасывает исключение для остановки графа. Код после него не выполняется |
| **Правило** | Данные для response готовить ДО interrupt или в предыдущей ноде. Паттерн: `explore` (готовит chartUrl) → `show_exploration` (только interrupt) |

### State params не propagate между nodes

| | |
|---|---|
| **Паттерн ошибки** | Инжектирую params (currentSearchParams) через test helper, но они теряются после первого node |
| **Первопричина** | Node не возвращает поле в return → LangGraph использует default из annotation (null). Params исчезают |
| **Правило** | Nodes которые НЕ меняют params должны возвращать их: `return { ...result, currentSearchParams: state.currentSearchParams }`. Альтернатива: parse node перезаписывает `newParams ?? state.params` |

### undefined теряется в JSON.stringify

| | |
|---|---|
| **Паттерн ошибки** | `answerText: state.currentAnswer` где currentAnswer может быть undefined |
| **Первопричина** | `JSON.stringify({a: undefined})` → `{}` (поле пропадает). NLP formatter не видит поле вообще |
| **Правило** | Явно конвертировать undefined в null: `answerText: state.currentAnswer ?? null`. JSON сохранит поле со значением null |

### Prompt "X FIRST, then Y" → LLM делает оба

| | |
|---|---|
| **Паттерн ошибки** | "Show answer FIRST, then brief results summary" — LLM показывал и answer и results |
| **Первопричина** | LLM буквально выполняет инструкцию. "FIRST, then" = последовательность, не выбор |
| **Правило** | Для условного выбора: "Show ONLY the answer. Do NOT show results/goal/filters." Явно указывать что НЕ делать |

### Debug от конца к началу

| | |
|---|---|
| **Паттерн ошибки** | Искал проблему в classification, хотя баг был в response-builder |
| **Первопричина** | Debug "по потоку" (от начала к концу). Но если конечный результат неверный, эффективнее идти от конца |
| **Правило** | При проблеме с output: (1) проверить что NLP formatter получил, (2) проверить что response-builder вернул, (3) потом classification. От конца к началу |

### Structured schema вместо prompt descriptions

| | |
|---|---|
| **Паттерн ошибки** | `preview: z.string().describe("YYYY-YYYY: Job Title, NO skills")` — LLM игнорирует description, добавляет скиллы |
| **Первопричина** | `.describe()` это hint, не constraint. LLM может интерпретировать по-своему |
| **Правило** | Для строгого контроля output — структурированная schema: отдельные поля (`startYear`, `endYear`, `title`), форматирование в TypeScript. Schema = contract, describe = hint |

### Empty array ≠ null для inheritance

| | |
|---|---|
| **Паттерн ошибки** | `citizenships ??= firstContext.citizenships` — не работает когда LLM вернул `[]` |
| **Первопричина** | `??=` срабатывает только для `null`/`undefined`, не для пустого массива. LLM может вернуть `[]` вместо `null` |
| **Правило** | Использовать `hasValue()` helper: проверяет `null`, `undefined`, `[]`, `""`. Паттерн: `if (!hasValue(x)) x = default` |

### Подыгрывание LLM при тестировании

| | |
|---|---|
| **Паттерн ошибки** | Отправлял скриптовые сообщения ("меняю domains на backend") вместо реакции на ответ бота |
| **Первопричина** | Хочу быстрее пройти flow. Но: (1) не тестирую реальный UX, (2) не замечаю проблемы в ответах |
| **Правило** | При ручном тестировании: читать ответ → реагировать по смыслу → не подыгрывать. Токсичный пользователь не знает "правильных" команд |

