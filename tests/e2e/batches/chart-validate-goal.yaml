name: chart-validate-goal
description: Chart генерируется для validate-goal (reverseSearchPathfinders)
locale: ru

steps:
  - message: Я middle backend разработчик в technology, США
    expect:
      phase: confirming_adhoc_context
      adhocContext.industry: technology

  - message: хочу стать senior
    expect:
      phase: showing_goal

  - message: покажи кто уже достиг такой цели
    expect:
      # >10 candidates → facets (Progressive Disclosure)
      phase: asking_after_validate_facets
      facets.totalCount: "!null"
