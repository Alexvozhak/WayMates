# MCP Tool: set_goal

**Priority**: 🔴 P0

Создать Facade MCP tool `set_goal` с NLP parsing и normalization. Session validation → LLM extraction (goal text → TargetContext) → Dictionaries normalization → Core.setGoal() → response.
