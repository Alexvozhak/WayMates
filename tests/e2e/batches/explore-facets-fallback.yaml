name: chart-facets-fallback
description: Facets fallback при >10 кандидатах в exploration (healthcare = 17)
locale: ru

steps:
  - message: найди похожих, я middle backend разработчик в healthcare, США
    expect:
      phase: confirming_adhoc_context
      adhocContext.position: middle
      adhocContext.industry: healthcare
      adhocContext.countryCode: US

  - message: покажи похожих
    expect:
      # healthcare имеет >10 контекстов → facets
      phase: showing_exploration_facets
      facets.totalCount: "!null"
