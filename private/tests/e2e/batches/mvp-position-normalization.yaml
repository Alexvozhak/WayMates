name: mvp-position-normalization
description: Test position extraction - should normalize to dictionary values
locale: ru

steps:
  - message: |
      хочу рассказать историю карьеры
      Работал тимлидом в компании DSSL с 2020 по 2023 год.
      Занимался бэкендом, Python и Kubernetes.
      В Москве, сфера технологий.
      Это всё.
    expect:
      phase: awaiting_plan_confirmation

  - message: ок
    expect:
      phase: awaiting_clarification

  - message: гражданство России
    expect:
      phase: awaiting_context_confirmation

  - message: да, всё верно
    expect:
      phase: awaiting_final_confirmation

  - message: сохранить
    expect:
      phase: saved
